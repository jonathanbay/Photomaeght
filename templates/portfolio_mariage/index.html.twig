{% extends 'base.html.twig' %}

{% block title %}Portfolio mariage - Photomaeght{% endblock %}

{% block body %}
    <main>
        <div class="retour">
            <a href="{{ path('app_accueil') }}"><p>Retour catégories portfolio</p></a>
        </div>
        <div class="galerie">
        
            {% for mariagePhoto in photosMariage %}
                <div class="containerImg">
                    <img src="/uploads/{{ mariagePhoto.photo}}" alt="photos_mariage" loading="lazy" >
                </div>
            {% endfor %}
        </div>
    </main>
{% endblock %}
