{% extends 'base.html.twig' %}

{% block title %}Portfolio evenements - Photomaeght {% endblock %}

{% block body %}
    <main>
        <div class="retour">
            <a href="{{ path('app_accueil') }}"><p>Retour catégories portfolio</p></a>
        </div>
        <div class="galerie">
        
            {% for portfolioEvenement in photosEvenement %}
                <div class="containerImg">
                    <img src="/uploads/{{ portfolioEvenement.photo}}" alt="photos_evenements" loading="lazy" >
                </div>
            {% endfor %}
        </div>
    </main>
{% endblock %}