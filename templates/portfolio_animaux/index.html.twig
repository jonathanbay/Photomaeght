{% extends 'base.html.twig' %}

{% block title %} PortFolio Animaux - Photomaeght {% endblock %}

{% block body %}
    <main>
        <div class="retour">
            <a href="{{ path('app_accueil') }}"><p>Retour catégories portfolio</p></a>
        </div>
        <div class="galerie">
        
            {% for animauxPhotos in photosAnimaux %}
                <div class="containerImg">
                    <img src="/uploads/{{ animauxPhotos.photo}}" alt="photos_animaux" loading="lazy" >
                </div>
            {% endfor %}
        </div>
    </main>
{% endblock %}
